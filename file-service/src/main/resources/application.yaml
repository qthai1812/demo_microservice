server:
  port: 8081
  servlet:
    context-path: /file

spring:
  application:
    name: file-service
  data:
    mongodb:
      uri: "${SPRING_DATA_MONGODB_URI:mongodb://root:root@localhost:27017/file-service?authSource=admin}"

jwt:
  signerKey: "${JWT_SIGNER_KEY:1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij}"

app:
  file:
    storage-dir: "${FILE_STORAGE_DIR:./uploads}"
    download-prefix: "${FILE_DOWNLOAD_PREFIX:http://localhost:8888/file/media/download/}"